width = float(input("กรุณาใส่ค่าความกว้าง: "))
length = float(input("กรุณาใส่ค่าความยาว: "))
height  = float(input("กรุณาใส่ค่าความสูง: "))
volume = width * length * height


# แสดงผลลัพธ์
print(f"ปริมาตรของสี่เหลี่ยมผืนผ้าคือ: {volume}")